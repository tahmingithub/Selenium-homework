We need Locator to find webelement to interact with them.
=-for locator always pick the unique name .
 for locator  never  pick the digit and space.
  LinkedText:
1: the element must be in "a" tag, a tag is also called anchor tag and it usually contains links
2: the element must have some text
if one of the conditions is not fullfiled you cant use the linktext locator.

 Partial Linktext:

1: the element must be in "a" tag, a tag is also called anchor tag and it usually contains links
2: the element must have some text
if one of the conditions is not fullfiled you cant use the linktext locator.
you can use partial text to locate elements.

//a      means  find me all tags which have a on them .

